# Example configuration for readarr-to-play-books

# Optional: single file to upload (Readarr post-import will set this)
# READARR_BOOKFILE_PATH=/books/Author/Title - Subtitle (Year)/Title - Subtitle - Author.epub

# Optional: scan directory fallback
# READARR_EXPORT_DIR=/books

# Chromium user data dir with saved Google session
USER_DATA_DIR=./user-data

# State file to track uploaded files
STATE_FILE=./uploaded_state.json

# Headless mode (true/false)
HEADLESS=true

# Upload batch size
BATCH_SIZE=3

